package org.firstinspires.ftc.teamcode;

public class Chassis {
    private Wheel wheels[];
    
    Chassis(String ids[]) {
        for (int i = 0; i < ids.len; i++) {
            Wheel newWheel = new Wheel(id);
        }
    }
}